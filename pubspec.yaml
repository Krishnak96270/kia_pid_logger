name: kia_pid_logger
description: Kia Sonet ELM327 PID Logger

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bluetooth_serial_plus: ^0.0.1
  sqflite: ^2.3.0
  path: ^1.9.0
  permission_handler: ^11.0.1
  share_plus: ^7.2.1
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
